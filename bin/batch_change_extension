#!/bin/bash

ls

read -p 'select files to change using regex:' filelist
read -p 'original extension (.txt):' origext
read -p 'new extension (.md):' newext

for f in $filelist; do 
mv -- "$f" "${f%$origext}$newext"
done
